[pytest]
DJANGO_SETTINGS_MODULE = mediflow.settings.test
python_files = test_*.py
markers =
    unit
    integration
    contract
    smoke
    concurrency
    repair
    regression
addopts = --reuse-db -v
